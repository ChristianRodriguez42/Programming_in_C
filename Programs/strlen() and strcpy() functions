#include <stdio.h>
#include <string.h>      // for strlen() and strcpy() functions
{
char after[4]; 
char name[40]; // name is an array of 40 chars

strcpy(after, "xyz");
strcpy(name, "mine");
int size, letters;
printf("Hi! What's your first name?\n");
scanf("%s", name);

size = sizeof(name);
letters = strlen(name);
printf(“Your first name has %d letters,\n",
letters);
printf("and we have %d bytes to store it.\n", size);
return 0;
}
