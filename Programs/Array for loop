#include <stdio.h>
#include <string.h>
int main(void)
{
	int myArray[5] = { 1,2,3,4,5 };
	for (int i = 0; i<5; i++)
	{
		printf("The value of array entry %d is %d\n", i, myArray[i]);
	}
	getchar();
	//printf("Where is my eye %d", i);
	return 0;

}
